ACTION_UNIQUEID: propose_threesome_best
ACTION_NAME: Have threesome
WHERE: interaction
WHEN: 0 - 24
MINUTES: 1 - 1
TIMEOUT_MINUTES: 0
ALSO_TIMEOUT: 
EFFECTS:
CONDITIONS:
MOVE_FIRST: true
SCENE_ALWAYS: propose_threesome_best
ANIMATION:
WHO: Actor = getTarget(); If Actor.isCreature() && Actor.isCreatureType(Dog) && Actor.hadSex() && Actor.isMale()
ACTION_UNIQUEID: master_to_slave
ACTION_NAME: Use my slave
WHERE: interaction
WHEN: 0 - 24
MINUTES: 1 - 1
TIMEOUT_MINUTES: 0
ALSO_TIMEOUT: 
EFFECTS:
CONDITIONS:
MOVE_FIRST: true
SCENE_ALWAYS: master_to_slave
ANIMATION:
WHO: Actor = getTarget(); If !Actor.isCreature() && Actor.hadSex() && !Actor.isMale()